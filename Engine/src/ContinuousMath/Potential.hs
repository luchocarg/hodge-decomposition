{-# LANGUAGE StrictData #-}

module ContinuousMath.Potential where
{- module ContinuousMath.Potential (
    solvePotentials
) where

import qualified Data.Map.Strict as Map
import qualified Domain.Types as Dom
import Data.List (foldl')
import Data.Maybe (fromMaybe)

-}